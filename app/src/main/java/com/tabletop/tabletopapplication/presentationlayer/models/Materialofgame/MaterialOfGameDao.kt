package com.tabletop.tabletopapplication.presentationlayer.models.Materialofgame
/*

import androidx.lifecycle.LiveData
import androidx.room.*

@Dao
interface MaterialOfGameDao {

    @Insert(onConflict = OnConflictStrategy.IGNORE)
    fun insert(materialOfGame: MaterialOfGame)

    @Delete
    fun delete(materialOfGame: MaterialOfGame)

    @Update
    fun update(materialOfGame: MaterialOfGame)

    @Query("Select * from materialOfGameTable order by id ASC")
    fun getAllMaterialOfGame(): LiveData<List<MaterialOfGame>>

    @Query("Select * from materialOfGameTable Where id=:id")
    fun getOneMaterialOfGame(id:Int): LiveData<MaterialOfGame>
}*/
